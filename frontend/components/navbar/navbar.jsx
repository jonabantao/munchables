import React from 'react';

const NavBar = (props) => {
  return (
    <nav className="navbar">
      This is a navbar.
      // Search Bar Container
      // SessionContainer
    </nav>
  );
};

export default NavBar;